import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import type { RelayRunArtifactPreview } from '../types'; import { Badge } from '@/components/ui/badge';
export function ArtifactPreviewCard({artifact}:{artifact:RelayRunArtifactPreview}){return <Card><CardHeader><CardTitle className="flex items-center justify-between text-base"><span>{artifact.kind}</span><Badge>{artifact.status}</Badge></CardTitle></CardHeader><CardContent><p className="text-sm text-slate-400">{artifact.filename}</p>{artifact.preview && <p className="mt-3 rounded-md bg-slate-900 p-3 text-sm">{artifact.preview}</p>}</CardContent></Card>}
