import { RELAY_RUN_STEPS, type RelayRunStepKey } from '../types';
export function RunStepper({activeStep, runId}:{activeStep:RelayRunStepKey; runId:string}){const activeIndex=RELAY_RUN_STEPS.findIndex(s=>s.key===activeStep);return <ol className="grid gap-3 md:grid-cols-4">{RELAY_RUN_STEPS.map((s,i)=><li key={s.key}><a href={`/runs/${runId}/${s.key}`} className={`block rounded-xl border p-4 ${i<=activeIndex?'border-cyan-700 bg-cyan-950/20':'border-slate-800 bg-slate-950'}`}><div className="text-xs text-slate-400">Step {i+1}</div><div className="font-semibold">Step {i+1} — {s.label}</div><p className="mt-1 text-xs text-slate-400">{s.description}</p></a></li>)}</ol>}
