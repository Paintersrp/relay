# Relay API base URL — Go backend running locally
# Pass 1: UI uses mock data only; this value is not used in any API call yet.
# Pass 3 will add read-only backend JSON endpoints that consume this.
VITE_RELAY_API_BASE_URL=http://localhost:8080
