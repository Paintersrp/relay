# Backend Code Surface Map

Canonical map path: `docs/backend-code-surface-map.md`.

Purpose: compiled backend routing index for backend/API/MCP/storage/drift/settings planning.

Authority note: source code, tests, selected handoffs or canonical packets, Relay DB state, run artifacts, audit evidence, and canonical relay-specs files override the map.

Usage rule: load the map before broad rediscovery, then verify current source and refresh stale rows only.

Do not duplicate the full map content here.
